node_modules/
.next/
out/
build/
html-export/

# Screen recordings kept alongside the project — not source, and large enough
# to bloat the repo permanently if committed.
*.mp4

# Personal/business documents that live in this folder but are not site assets.
*.pdf
.DS_Store
*.pem
.env*.local
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
